#include "../includes/VirtualServer.hpp"

// Constructors
VirtualServer::VirtualServer(Server server, std::string &config_str)
{
	(void)server;
	(void)config_str;
}

// Destructor
VirtualServer::~VirtualServer(void) {}
